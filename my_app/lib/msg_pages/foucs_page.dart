import 'package:flutter/material.dart';
class FoucsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child:Center(
        child: Text('暂无消息'),
      ),
    );
  }
}